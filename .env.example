# ------------------------------------------------------------------
#  .env ファイルのテンプレート
# ------------------------------------------------------------------
# このファイル（.env.example）をコピーして .env という名前のファイルを作成し、
# 以下の <...> の部分にあなたのローカル環境の値を設定してください。
# .env ファイルは .gitignore に追加し、Gitで管理しないでください。
# ------------------------------------------------------------------


# === データベース (MySQL) 設定 ===

# MySQLのrootユーザーのパスワード（ローカル開発用なので推測されにくいもの）
MYSQL_ROOT_PASSWORD=<your_root_password>

# 作成するデータベースの名前
MYSQL_DATABASE=kakeibo_db

# アプリケーションが使用するMySQLユーザー名
MYSQL_USER=<your_app_user>

# アプリケーションが使用するMySQLユーザーのパスワード
MYSQL_PASSWORD=<your_app_password>


# === バックエンド (FastAPI) 設定 ===

# FastAPIがMySQLデータベースに接続するためのURL
#
# 書式: mysql+pymysql://<ユーザー名>:<パスワード>@<ホスト名>:<ポート>/<DB名>
#
# Docker Compose を使う場合、ホスト名は 'localhost' ではなく、
# docker-compose.yml で定義したDBサービスの'サービス名'（例: 'db'）になります。
#
# 上記の <your_app_user> と <your_app_password> に設定した値を
# 以下のプレースホルダーにも当てはめてください。
DATABASE_URL=mysql+pymysql://<your_app_user>:<your_app_password>@db:3306/kakeibo_db